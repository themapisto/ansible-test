[Unit]
Description={{ tomcat7_service_name }}
After=network.target

[Service]
Type=forking
User={{ tomcat_user }}
Group={{ tomcat_group }}

Environment=JAVA_HOME={{ __java_home }}
Environment=CATALINA_PID={{ tomcat7_home }}/{{ tomcat7_service_name }}.pid


ExecStart={{ tomcat7_home }}/bin/startup.sh
ExecStop=/bin/kill -15 $MAINPID

[Install]
WantedBy=multi-user.target